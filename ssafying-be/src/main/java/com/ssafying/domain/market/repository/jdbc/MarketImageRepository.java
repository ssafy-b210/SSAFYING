package com.ssafying.domain.market.repository.jdbc;

import com.ssafying.domain.market.entity.MarketImage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MarketImageRepository extends JpaRepository<MarketImage, Integer> {
}
