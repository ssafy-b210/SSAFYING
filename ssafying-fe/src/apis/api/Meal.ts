import { axios } from "../utils/axios";

const REST_MARKET_API = `/api/meal`;

export async function selectMeal(userId: number) {
  try {
  } catch (error) {
    console.error(error);
    throw error;
  }
}
